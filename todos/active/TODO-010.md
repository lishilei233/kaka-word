### TODO-010：记录匿名聚合的识别纠错与候选确认数据

- 类型：feature
- 优先级：P2
- 状态：已完成
- 需求描述：
  - 新增数据库表，按日期聚合保存原始识别词、用户修改后的词和修改次数。
  - 记录候选确认结果，包括用户选择的候选、是否选择第一候选，以及是否选择“其他”。数据应能区分第一候选、第二/第三候选和“其他”。
  - iOS 在用户成功选择候选或保存手动修改后上报统计事件；服务端验证字段后，以聚合更新方式写入计数。
  - 只保存匿名聚合数据，不保存照片、用户身份、设备 ID、例句、完整请求或可关联请求标识。
  - 英文词统一转为小写，中文去除首尾空格；长度限制与现有词汇接口一致。
  - 统计上报或写入失败不得影响识别结果、候选选择或历史记录保存；老版本 App 不上报新字段时保持兼容。
- 简要分析：
  - 用户事实：目标是积累可人工分析的易混淆词组与候选确认分布，不要求系统自动修改提示词或自动判断模型准确率。
  - 初步分析：该功能横跨 iOS 事件触发、服务端字段验证与数据库按日聚合写入，适合作为独立于识别主流程的非阻塞统计链路。
  - 初步分析：为同时满足“用户选择的候选”和候选位次统计，事件及聚合维度可能需要表达候选词、候选序号以及“其他”类别；具体字段和唯一约束待技术分析时结合现有数据层确定。
  - 初步分析：需求中的“重复上报不会导致请求失败”明确要求接口可接受重复事件并通过聚合更新计数，但未要求去重；在不保存可关联请求标识的前提下，重复提交是否允许重复计数需在实现前确认。
- 影响范围：
  - 数据库：新增按日期、纠错词对及候选确认维度聚合的表和迁移。
  - 服务端：新增或扩展统计上报接口、字段校验、词汇规范化、长度限制复用、聚合更新与失败容错测试。
  - iOS：候选选择成功和手动修改保存成功后的非阻塞统计上报，以及旧服务端或上报失败时的降级行为。
  - 隐私与数据治理：确保数据库、日志和事件载荷均不包含照片、用户身份、设备 ID、例句、完整识别请求或可关联请求标识。
- 验收标准：
  - 能在数据库中查询指定日期的原始识别词、用户修改后词及累计修改次数。
  - 能查询并区分第一候选、第二候选、第三候选和“其他”的确认结果；同时可判断确认项是否为第一候选。
  - iOS 仅在候选成功选择或手动修改成功保存后发起统计上报；上报失败不改变识别结果、不阻止候选选择，也不影响历史记录保存。
  - 服务端对字段进行验证和规范化：英文词统一小写，中文去除首尾空格，长度限制与现有词汇接口一致。
  - 服务端采用数据库聚合更新累计次数；重复上报不会导致接口或用户主流程失败。
  - 老版本 App 不提供统计事件或新字段时，现有识别、候选确认和历史记录流程保持兼容。
  - 数据库记录、统计载荷及相关日志中不包含照片、用户身份、设备 ID、例句、完整请求或可关联请求标识。
  - 本条目不实现自动修改提示词、自动判定模型准确率、保存原始图片或可回放样本，也不新增管理后台或报表页面。
- 备注：
  - 关联：TODO-004（识别候选确认与用户纠错交互）。本条目只登记后续匿名统计能力，不改变 TODO-004 当前的待验证状态。
  - 后续用途：累计数据由人工确认易混淆词组，提示词调整另行登记和实施。
  - 已确认（2026-09-07）：允许重复上报重复计数；接口成功响应与主流程解耦，不实现同一次用户操作的去重。
  - 优先级暂定 P2：该能力用于后续分析，不阻塞当前识别与纠错主流程。

- 技术分析（2026-09-07）：
  - 复用现有 access token 鉴权，但新增独立的识别反馈接口和 `AccessService.recordRecognitionFeedback`，不扩展订阅指标的 `productId/outcome` 维度承载词汇数据。
  - 新增按日聚合的候选确认表和纠错表；候选选择使用 `first/second/third/other`，第一候选由选择类型直接判断，非第一候选同时累加纠错次数。
  - iOS 在候选或“其他”确认经过现有结果更新回调成功后异步上报；上报错误被吞掉，不影响 Sheet 关闭、识别结果和历史保存。
  - 载荷只包含原始及选择后的中英文词和选择类型；服务端执行英文小写化、中文首尾空格清理和 1–60 字符校验，不接收照片、例句、音标、边界框或请求关联标识。
  - 新反馈接口的请求日志不输出 request ID、请求体或词汇内容；老版本客户端继续不调用该接口，不影响既有识别和历史流程。

- 执行记录（2026-09-07）：
  - 状态：已完成
  - 分支：`feature/TODO-010-记录匿名聚合的识别纠错与候选确认数据`
  - 实现摘要：新增 004 数据库迁移、候选确认与纠错按日聚合表、独立识别反馈接口和 PostgreSQL 事务聚合更新；允许重复上报重复计数。iOS 在候选确认和单词详情手动修改成功更新后，以非阻塞方式上报原始/选择中英文词及 `first/second/third/other` 类型。
  - 隐私处理：反馈接口不返回或记录 request ID，不记录照片、例句、音标、边界框、设备 ID、用户身份或完整请求。
  - 涉及范围：`server/migrations/004_recognition_feedback.sql`、`server/scripts/migrate.mjs`、`server/src/routes/recognition-feedback.ts`、`server/src/core/access/`、`ios/Core/Networking/APIClient.swift`、`ios/Core/Purchases/MembershipStore.swift`、`ios/Features/Result/`。
  - 验证：`npm run build` 通过；`npm test` 45/45 通过；识别反馈接口测试 2/2 通过并已纳入主测试脚本；PostgreSQL 集成测试因未配置 `TEST_DATABASE_URL` 跳过；`git diff --check` 通过。
  - 环境限制：`xcodebuild` 无法完成 Asset Catalog 编译，当前环境没有可用的 iOS Simulator runtime；尚未完成 iOS 编译、真机和 UI 回归验证。
  - 后续事项：无。生产部署前按环境执行数据库迁移即可。

- 验收记录（2026-09-08）：
  - 用户确认 TODO-010 已验收完成。
  - 按当前约定允许重复上报重复计数；条目状态更新为 `已完成`。
